# Zone B Runtime Alignment Audit

## Runtime entry

Zone A resolves its issue in `app/innerwork/page.tsx`:

1. `dailyGuidance.dominantIssue.key`
2. Text inference from Catatan
3. Profile/wellness/navigator derivation
4. `difficulty_resting` fallback

That key is passed to `buildInnerworkDailyDecision()`.

## Exact source by category

| Zone B category | Card source | Destination runtime source |
|---|---|---|
| Journaling | Issue-specific `SUPPORT_LIBRARY` | Independent journal theme generator |
| Meditation | Issue-specific `SUPPORT_LIBRARY` | Blueprint/date/history meditation generator |
| Breathwork | Issue-specific `SUPPORT_LIBRARY` | Redirects to general Meditation page |
| Mudra | Issue-specific `SUPPORT_LIBRARY` | Mudra selected by independent Meditation theme |
| Yoga | Issue-specific `SUPPORT_LIBRARY` | Entire static `YOGA_DATABASE` |
| Workout | Not generated by support decision | Static hub link → workout databases |
| Audio Healing | Issue-specific `SUPPORT_LIBRARY` | Fixed embedded playlist |

## Difficulty Resting runtime

The decision engine produced:

- Journaling: `Izin untuk Berhenti`
- Meditation: `Meditasi Istirahat Tanpa Syarat`
- Breathwork: `Downshift Breath`
- Mudra: `Apana Mudra`
- Yoga: `Savasana`
- Audio: `Sleep and Rest Healing`

The card labels are aligned. No destination link carries `practiceId`, issue, or theme. Destination pages regenerate or list unrelated content.

## Family Dynamics explanation

When the user clicks the aligned Journaling card:

```text
Difficulty Resting
→ support card “Izin untuk Berhenti”
→ /innerwork/journaling
→ getTodayJournalPrompt(context, entries)
→ date + life path + arcana + HD type + entry count
→ one theme from the full static THEME_BANK
→ “Family Dynamics” can be selected
```

The Journaling page never receives `difficulty_resting` or `difficulty-resting-journal`.

## Verdict

**LEGACY LEAKING**

Zone B card generation is issue-aware, but actual category experiences are not connected to those recommendations.
