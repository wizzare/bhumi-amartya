# INNERWORK ASTRO SOURCE USAGE

Analysis of how celestial events influence today's practice.

| Astro Item | Status | Impact |
|---|---|---|
| **Moon Phase** | **USED (via Catatan)** | Phase logic from `DailyNoteV2` informs the `fallbackFocus`. |
| **Western Transits** | **USED (via Engine)** | `innerworkIntelligence` uses `activations` to weight items. |
| **BaZi / Tzolkin / Jawa** | **NOT USED** | Currently bypassed at the practice recommendation level. |
| **Eclipse Awareness** | **BYPASSED** | Not explicitly used to gate practices in the current runtime. |

**Observation:** Astro currently serves as a **contextual amplifier**. It doesn't choose the practice, but it validates the "Why" (e.g., "The current sky makes this pattern more visible").
