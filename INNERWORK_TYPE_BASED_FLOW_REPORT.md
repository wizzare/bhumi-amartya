# Innerwork Type-Based Flow Report

Implemented chain:

Catatan dominant issue → normalized issue key → navigator-specific type → exact complete practice → UI completion → full Journey record → recent-history input for the next recommendation.

Required issue checks pass at source level:

- Love Block → relationship-based type
- Inner Child → inner-child type
- Money Block → money/safety type
- Low Energy → recovery/body type
