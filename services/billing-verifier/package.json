{
  "name": "bhumi-billing-verifier",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "lint": "eslint api lib",
    "db:migrate": "tsx scripts/migrate.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "firebase-admin": "^13.10.0",
    "google-auth-library": "^10.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^5.3.0",
    "eslint": "^9.0.0",
    "typescript": "^5.0.0"
  }
}
