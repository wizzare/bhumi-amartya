# Data Assets

This directory contains static data files used by the application, loaded at runtime via `importlib.resources`.

## Contents

- **`layout_data.json`**: Contains the SVG path data and coordinate definitions for rendering the Human Design BodyGraph. This includes:
    - Center shapes and positions.
    - Gate positions and text coordinates.
    - Channel paths (connectors between centers).
