# Innerwork Zone B Runtime Trace

## Current logged-in user

Runtime values could not be observed. The browser runner failed before launch, so the current account's:

- dominant issue
- Zone A practice
- navigator
- six Zone B items

are **UNVERIFIED**.

## Certain source trace

Zone A:

`Catatan explicit issue → Catatan keyword issue → profile/wellness fallback → normalizeIssue → TYPE_BASED_PRACTICES → mainPractice`

Zone B:

`mainPractice.issueKey → supportForIssue(issueKey) → SUPPORT_LIBRARY[issueKey] → one item per category`

Categories:

- Journaling
- Meditation
- Breathwork
- Mudra
- Yoga
- Audio

## Dominant issue or fallback library?

For all 15 declared master issues, Zone B uses the same issue key as Zone A.

Fallback occurs only when `normalizeIssue()` receives an unknown key. Unknown issues become `low_energy`, then Zone B uses the Low Energy library.

## Dynamic reality

Each issue/category currently contains one support candidate. Date seed, helped score, heavier score, and recent-ID ranking cannot select a different item because the candidate array length is one.

Thus Zone B is:

- issue-specific: **YES**
- dynamically regenerated into different content: **NO**
- Journey-adaptive in visible selection: **NO**
