# Innerwork Catatan Contract Repair

`DailyGuidance` now supports an explicit Catatan-owned `dominantIssue` contract.

Innerwork priority:

1. `dailyGuidance.dominantIssue.key`
2. Deterministic issue extraction from existing Catatan categories/text for legacy records
3. Structured profile/wellness fallback only when Catatan has no identifiable issue

Navigator never replaces the issue. Love Block remains relationship-based, Money Block remains money/safety-based, and Inner Child remains inner-child-based.
